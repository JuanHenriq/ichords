<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_height="match_parent"
    android:background="#F8F8F8">

    <ImageView
        android:id="@+id/logoepesq"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:src="@drawable/cabecalho"
        android:scaleType="fitCenter"
        android:layout_alignParentTop="true" />

    <ImageView
        android:id="@+id/heartfavorite"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/icon_heart_favorite"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/logoepesq"
        android:layout_marginTop="40dp" />

    <TextView
        android:id="@+id/infoTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Você ainda não possui nenhuma música entre suas favoritas."
        android:textSize="16sp"
        android:textColor="#000000"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/heartfavorite"
        android:layout_marginTop="60dp"
        android:layout_marginStart="20dp"
        android:layout_marginEnd="50dp"
        android:gravity="center" />

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        app:menu="@menu/bottom_navigation_menu"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        app:labelVisibilityMode="unlabeled"
        app:itemIconTint="#0068FF"
        android:layout_alignParentBottom="true"
        android:background="@android:color/white"
        />

</RelativeLayout>
